<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#001C3E"
    tools:context=".MainActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true">

        <ImageView
            android:id="@+id/imgBackMain"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/altBackgroundMain"
            android:fadingEdge="vertical"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/img_naruto_silhueta" />

    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="bottom"
        android:paddingBottom="@android:dimen/thumbnail_width">

        <Button
            android:id="@+id/btnInfo"
            android:layout_width="@android:dimen/thumbnail_width"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:background="@drawable/botao_design1"
            android:drawableStart="@drawable/ic_folha_symbol"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/bt_info" />

        <Button
            android:id="@+id/btnStart"
            android:layout_width="@android:dimen/thumbnail_width"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:background="@drawable/botao_design1"
            android:drawableEnd="@drawable/ic_naruto_temaki"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/bt_comeco"
            android:onClick="startQuiz"/>
    </LinearLayout>

</android.support.constraint.ConstraintLayout>